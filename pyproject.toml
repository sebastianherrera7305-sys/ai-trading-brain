[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-brain"
version = "0.1.0"
description = "Market structure detection: swing points, HH/HL/LH/LL, BOS and CHoCH. Pure logic, no broker."
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["trading_brain"]
